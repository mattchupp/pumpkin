/*

export default async function tickets(req, res) => {

  // Processing post request
  res.setHeader('Content-Type', 'application/json')
  if(req.method === 'POST') {
    try {

    } catch (error) {
      console.log(error);
      res.status(error.status || 500).end(error.message);
    }
  }

}


*/
